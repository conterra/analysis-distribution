{
    "dataform-version": "1.0.0",
    "type": "gridpanel",
    "showLabels": true,
    "children": [
        {
            "type": "selectbox",
            "field": "storeId",
            "label": "${store}",
            "searchAttribute": "title",
            "labelAttribute": "${title}",
            "store": "storesStore01",
            "size": {
                "w": 150
            }
        },
        {
            "type": "selectbox",
            "field": "chartType",
            "label": "${defaultChartType}",
            "searchAttribute": "name",
            "labelAttribute": "${name}",
            "values": [{
                    "name": "${column}",
                    "value": "column"
                }, {
                    "name": "${pie}",
                    "value": "pie"
                }
            ],
            "max": 2,
            "size": {
                "w": 150
            }
        },
        {
            "type": "selectbox",
            "field": "spatialOperator",
            "label": "${spatialOperator}",
            "searchAttribute": "name",
            "labelAttribute": "${name}",
            "values": [{
                    "name": "intersects",
                    "value": "$intersects"
                }, {
                    "name": "contains",
                    "value": "$contains"
                }
            ],
            "max": 2,
            "size": {
                "w": 150
            }
        },
        {
            "type": "checkbox",
            "label": "${useExtent}",
            "field": "useExtent"
        },
        {
            "type": "checkbox",
            "label": "${enableChartSwitch}",
            "field": "enableChartSwitch"
        },
        {
            "type": "checkbox",
            "label": "${enableExtentSwitch}",
            "field": "enableExtentSwitch"
        }
    ]
}